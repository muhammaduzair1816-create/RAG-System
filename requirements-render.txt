# Deployment manifest for Render / any 512 MB container host.
#
# Identical to requirements.txt except that sentence-transformers (and its torch
# dependency, ~2 GB of build artefacts) is omitted. The default "onnx" embedding
# backend runs the same all-MiniLM-L6-v2 checkpoint and produces identical
# vectors, so nothing about retrieval changes.
#
# Use requirements.txt for local development if you want the torch backend.

# --- Interface ---
streamlit>=1.40.0

# --- PDF text extraction ---
pypdf>=5.1.0

# --- OCR for scanned PDFs (system binaries come from the Dockerfile) ---
pytesseract>=0.3.13
pdf2image>=1.17.0
Pillow>=10.4.0

# --- Embeddings (onnx backend) ---
onnxruntime>=1.19.0
tokenizers>=0.20.0
huggingface_hub>=0.26.0

# --- Speech-to-text ---
faster-whisper>=1.1.0

# --- Vector database ---
pinecone>=5.4.0

# --- LLM API ---
groq>=0.13.0

# --- Utilities ---
python-dotenv>=1.0.1
numpy>=1.26.0
